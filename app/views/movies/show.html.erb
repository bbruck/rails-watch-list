<div class="show-container">
  <br>
  <h1>Details for "<%= @movie.title %>"</h1>
  <p><strong>Short description:</strong> <%= @movie.overview %>
  <%= image_tag @movie.poster_url, alt: @movie.title, width: 200 %>
</div>
